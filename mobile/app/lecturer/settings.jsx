import { View } from 'react-native';

/**
 * Dummy settings route — see _layout.jsx for the fake-tab pattern.
 * The Settings tab opens a popover instead of navigating; this file
 * exists so the route name resolves.
 */
export default function SettingsScreen() {
  return <View />;
}